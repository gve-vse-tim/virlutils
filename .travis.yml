language: python
python:
  - "2.7"
install: "pip install -r test-requirements.txt"
script:
  - make lint
  - python setup.py build
  - python setup.py install
  - make coverage
  - pip freeze
after_success:
  - coveralls
