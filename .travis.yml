language: python
python:
  - "2.7"
install: "pip install -r test-requirements.txt"
script:
  - mkdir -p .virl/default
  - make lint
  - make coverage
  - pip freeze
  - ulimit -a
after_success:
  - coveralls
